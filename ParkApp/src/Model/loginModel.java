package Model;

/**
 * this will be the class for verifying login information based off of a database
 * @author hayde
 */
public class loginModel {
    
    /**
     * will verify if the user exists and has data to access
     * @return a variable of User type
     */
     public User verifyUser(){
            
        
    }
    
     /**
      * allows the user to create a new member of database
      * @return a variable of User type
      */
     public User createUser(){
     }
     
}
